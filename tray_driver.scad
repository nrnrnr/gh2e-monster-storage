use <well.scad>         // hand-written well-building module

use <outlines.scad>     // path-returning function for each monster
use <tray_modules.scad> // coordinates for each monster
                        // polygon-building function for each monster,
                        // each with rotation from tray.tex

 // this is tray.scad, which is elements, plus one module per try

tray1();
